#!/usr/bin/with-contenv bash

#======================================================================================================================
# Reload nginx - but only if it is running
#======================================================================================================================

if [ ! -z "$(pidof nginx)" ] ; then
    _echo "Reloading nginx..."
    nginx -t && nginx -s reload
    _done
fi
