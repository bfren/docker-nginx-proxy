#!/usr/bin/with-contenv bash

set -euo pipefail


#======================================================================================================================
# Delete Nginx configuration files and reinitialise
#======================================================================================================================

_echo "Removing nginx configuration files..."
[[ ! -z "${SITES}" ]] && rm ${SITES}/* || _error "SITES is not defined."
_done

ssl-init
